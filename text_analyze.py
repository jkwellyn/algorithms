# create a function that takes a string
# filter the string  to count every two words that are adjacent by a white space should be a tuple
# add each unique tuple into an hashmap
# iterate over string to see how many times the tuples appear in the input, update counter for each tuple
# if word is separated by whitespace from the previous word, that is a tuple



def sort_into_tuple(self, input: str):
    words = input.split()




if __name__ == "__main__":

    test_string = "Fizz buzz buzz plop buzz fizz, suzz suzz fizz buzz fizz”
    
    tuples = []
    for i in range(len(test_string)-1):
        
        if len[tuples] == 0:
            test_string[i]

